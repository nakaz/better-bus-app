'use strict';

import React, {
  StyleSheet,
} from 'react-native';

export const Views = StyleSheet.create({
  description: {
        fontSize: 20,
        color: '#80CBC4'
    },
    container: {
        flex: 1,
        backgroundColor: '#263238',
        justifyContent: 'center',
        alignItems: 'center'
    }
});
